@using PagedList
@using PagedList.Mvc
@using PhotoContest.App.Models.Contest
@model IPagedList<SummaryContestViewModel>

@{
    ViewBag.Title = "My Contests";
}

<div class="col-md-7 col-md-offset-1">
    <h1>My contests</h1>
    <div class="row">
        @Html.DisplayForModel()
    </div>
    @Html.PagedListPager(Model, page => Url.Action("Contests", new { page, ViewBag.sortBy }))
</div>

